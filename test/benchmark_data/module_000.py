"""Module 0"""

def compute_0_0(a, b, c):
    x = a * 1 + b * 2
    y = c * 3 - a * 4
    for i in range(5):
        x = x + i * 1
        y = y - i * 1
        if x > 5000:
            x = x % 1000
    return x + y + 1


def compute_0_1(a, b, c):
    x = a * 4 + b * 9
    y = c * 8 - a * 15
    for i in range(6):
        x = x + i * 2
        y = y - i * 3
        if x > 5010:
            x = x % 1005
    return x + y + 1


def compute_0_2(a, b, c):
    x = a * 7 + b * 16
    y = c * 13 - a * 26
    for i in range(7):
        x = x + i * 3
        y = y - i * 5
        if x > 5020:
            x = x % 1010
    return x + y + 1


def compute_0_3(a, b, c):
    x = a * 10 + b * 23
    y = c * 18 - a * 37
    for i in range(8):
        x = x + i * 4
        y = y - i * 7
        if x > 5030:
            x = x % 1015
    return x + y + 1


def compute_0_4(a, b, c):
    x = a * 13 + b * 30
    y = c * 23 - a * 48
    for i in range(9):
        x = x + i * 5
        y = y - i * 9
        if x > 5040:
            x = x % 1020
    return x + y + 1


def compute_0_5(a, b, c):
    x = a * 16 + b * 37
    y = c * 28 - a * 59
    for i in range(10):
        x = x + i * 6
        y = y - i * 11
        if x > 5050:
            x = x % 1025
    return x + y + 1


def compute_0_6(a, b, c):
    x = a * 19 + b * 44
    y = c * 33 - a * 70
    for i in range(11):
        x = x + i * 7
        y = y - i * 13
        if x > 5060:
            x = x % 1030
    return x + y + 1


def compute_0_7(a, b, c):
    x = a * 22 + b * 51
    y = c * 38 - a * 81
    for i in range(12):
        x = x + i * 8
        y = y - i * 15
        if x > 5070:
            x = x % 1035
    return x + y + 1


def compute_0_8(a, b, c):
    x = a * 25 + b * 58
    y = c * 43 - a * 92
    for i in range(13):
        x = x + i * 9
        y = y - i * 17
        if x > 5080:
            x = x % 1040
    return x + y + 1


def compute_0_9(a, b, c):
    x = a * 28 + b * 65
    y = c * 48 - a * 103
    for i in range(14):
        x = x + i * 10
        y = y - i * 19
        if x > 5090:
            x = x % 1045
    return x + y + 1


def compute_0_10(a, b, c):
    x = a * 31 + b * 72
    y = c * 53 - a * 114
    for i in range(15):
        x = x + i * 11
        y = y - i * 21
        if x > 5100:
            x = x % 1050
    return x + y + 1


def compute_0_11(a, b, c):
    x = a * 34 + b * 79
    y = c * 58 - a * 125
    for i in range(16):
        x = x + i * 12
        y = y - i * 23
        if x > 5110:
            x = x % 1055
    return x + y + 1


def compute_0_12(a, b, c):
    x = a * 37 + b * 86
    y = c * 63 - a * 136
    for i in range(17):
        x = x + i * 13
        y = y - i * 25
        if x > 5120:
            x = x % 1060
    return x + y + 1


def compute_0_13(a, b, c):
    x = a * 40 + b * 93
    y = c * 68 - a * 147
    for i in range(18):
        x = x + i * 14
        y = y - i * 27
        if x > 5130:
            x = x % 1065
    return x + y + 1


def compute_0_14(a, b, c):
    x = a * 43 + b * 100
    y = c * 73 - a * 158
    for i in range(19):
        x = x + i * 15
        y = y - i * 29
        if x > 5140:
            x = x % 1070
    return x + y + 1


def compute_0_15(a, b, c):
    x = a * 46 + b * 107
    y = c * 78 - a * 169
    for i in range(20):
        x = x + i * 16
        y = y - i * 31
        if x > 5150:
            x = x % 1075
    return x + y + 1


def shared_util_16(data):
    if data is None:
        raise ValueError("data required")
    result = []
    for item in data:
        if item is not None:
            val = str(item).strip()
            if len(val) > 0:
                result.append(val)
    return result


def shared_util_17(data):
    if data is None:
        raise ValueError("data required")
    result = []
    for item in data:
        if item is not None:
            val = str(item).strip()
            if len(val) > 0:
                result.append(val)
    return result


def shared_util_18(data):
    if data is None:
        raise ValueError("data required")
    result = []
    for item in data:
        if item is not None:
            val = str(item).strip()
            if len(val) > 0:
                result.append(val)
    return result


def shared_util_19(data):
    if data is None:
        raise ValueError("data required")
    result = []
    for item in data:
        if item is not None:
            val = str(item).strip()
            if len(val) > 0:
                result.append(val)
    return result


